## Regex in Python

This project uses Python's re library to go through SMS transaction messages and extract transaction details.
Given a list of SMS messages, the script identifies and separates income and expense transactions using regex patterns, then reports:

-The number of transactions for each category
-The total amount (in RWF) for income and expenses

